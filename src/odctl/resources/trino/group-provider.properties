# Group membership, so a rule can key on "group" rather than naming every user.
# Without a provider configured, no user is ever in a group and a group-keyed
# rule can never match.
group-provider.name=file
file.group-file=/etc/trino/groups.txt
